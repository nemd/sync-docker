version: '1.0'
steps:
    build-step:
        type: build
        image-name: nemd/resilio
        dockerfile: Dockerfile
        tag: latest

    test-all-the-code:
      image-name: nemd/resilio
      working-directory: ${{initial-clone}}
      commands:
        - ps axf
